spring:
  profiles: docker
  data:
    mongodb:
      host: mongo-user
      port: 27017

server:
  port: 8080

eureka:
  instance:
    preferIpAddress: true
  client:
    serviceUrl:
      defaultZone: http://discovery:8761/eureka/
